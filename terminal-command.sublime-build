{
    "cmd": ["bash","-c", "chmod +x '$file' && open -a Terminal '$file'"],
    "selector": "source.command",
    "variants":
    [
        {
            "name": "Run",
            "cmd": ["bash","-c", "chmod +x '$file' && open -a Terminal '$file'"]
        }
    ],
    "working_dir": "${project_path}"
}
